## CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository currently contains no source code. The only tracked file is `README.md`, which holds just the project title (`# test`). There is no build system, dependency manifest, test suite, lint configuration, or CI workflow yet.

As a result, there are no project-specific build/test/lint commands to document. When the user adds code, update this file with:
- The actual build, test, and lint commands (including how to run a single test).
- The high-level architecture once multiple files exist and the "big picture" requires cross-file reading to understand.

Until then, do not fabricate commands, frameworks, or architectural notes — ask the user what they want to build before scaffolding anything.

## Branch convention

Development for AI-assisted changes happens on the branch `claude/add-claude-documentation-6a175` (see the session instructions). Push to that branch; do not push to `main` without explicit permission.
